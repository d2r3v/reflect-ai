# Reflect AI: Memory-Aware Support Companion

![Architecture: FastAPI + React Native](https://img.shields.io/badge/Architecture-FastAPI_%7C_React_Native-blue)
![Auth: JWT](https://img.shields.io/badge/Auth-JWT_Bearer-green)
![Safety: Keyword Classifier](https://img.shields.io/badge/Safety-Classifier_%7C_Crisis_Bypass-orange)
![LLM: OpenAI](https://img.shields.io/badge/LLM-OpenAI_%7C_Mode--Aware-purple)

**Reflect AI** is a memory-aware support companion that persists context across conversations and adapts responses based on user state and safety signals.

Instead of treating each message as isolated, it builds a structured understanding of the user over time - enabling more consistent, contextual, and safer interactions.

## What it does

- Maintains conversation memory across sessions
- Retrieves relevant past context when generating responses
- Routes responses through safety-aware modes (e.g., normal, supportive, crisis)
- Provides a structured backend for building reliable, stateful AI systems

**Example flow:**
1. User sends a message
2. System retrieves relevant memory
3. Safety layer classifies risk level
4. Response mode is selected
5. LLM generates a context-aware reply
6. Message and context updates are persisted

## Motivation

Most AI chat systems treat conversations as stateless interactions.
Reflect AI explores how persistent memory, structured execution, and safety-aware routing can create more reliable and context-aware AI systems.

The goal is not just to generate responses, but to build a system that:
- remembers
- reasons about context
- and behaves safely under different conditions

## System Architecture

The project is built with a clear separation between mobile client and backend logic:

- **Backend (`/backend`)**: Built with **FastAPI**. It handles authentication, AI model orchestration, and memory management. 
  - A modular execution pipeline (context → tools → response) designed for future orchestration and multi-step reasoning.
- **Frontend (`/mobile`)**: Built with **React Native (Expo)**. Features end-to-end authentication, secure on-device state management, and a protected navigation stack.

## Key Features

- **Working End-to-End Flow**  
  Authenticated users can create conversations, send messages, and receive real LLM-generated responses via a live backend API.

- **Mode-Aware Response Generation**  
  Responses are generated by `CompanionBrain`, which selects a distinct system prompt per `ResponseMode` (reflect, vent, plan, grounding). Crisis mode bypasses the LLM entirely with a deterministic 988 response.

- **Sticky Crisis Cooldown**  
  Crisis safety state persists across messages. The system won't exit `post_crisis` mode until the classifier confirms 3 consecutive low-risk messages and 5 minutes have elapsed since the crisis event.

- **Crisis-Aware UI**  
  The chat interface renders a warm crisis card with a direct 988 CTA on crisis messages, and a softer post-crisis banner that stays throughout the cooldown period.

- **Safety Event Logging**  
  Every elevated-risk event is logged to the database with `risk_level`, `response_mode`, and `crisis_bypass` flag for future auditing.

- **End-to-End Authentication**  
  JWT-based auth with secure on-device token storage and automatic session restoration.

- **Extensible Execution Architecture**  
  `LLMProvider` abstractions allow provider swaps (OpenAI → Anthropic → Ollama). `Brain` ABCs allow adding new response strategies without touching business logic.

## Quick Start & Setup

### 1. Backend Setup

The backend runs natively on Python.

```powershell
cd backend
python -m venv venv-win
.\venv-win\Scripts\Activate.ps1
pip install -r requirements.txt

# Configure secrets
copy .env.example .env
# Edit .env to set JWT_SECRET and OPENAI_API_KEY

# Run migrations and start the server
alembic upgrade head
uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
```

*Note: Binding to `0.0.0.0` ensures the server is accessible to mobile emulators and physical devices over your local network. Configure secrets via `.env` based on `.env.example`.*

### 2. Frontend Setup

Requires Node.js and Expo CLI.

```bash
cd mobile
npm install

# Start the Expo development server
npx expo start --android
```

## Project Structure

```text
reflect-ai/
├── backend/
│   ├── src/
│   │   ├── core/
│   │   │   ├── auth/           # JWT security + FastAPI dependencies
│   │   │   ├── brains/         # Brain ABC, CompanionBrain, CrisisBrain, BrainRouter
│   │   │   ├── context/        # ExecutionContext, ResponseMode
│   │   │   ├── llm/            # LLMProvider ABC, OpenAIProvider
│   │   │   └── tools/          # Tool abstraction + ToolRegistry (future)
│   │   ├── db/                 # SQLAlchemy models + Alembic migrations
│   │   ├── routes/             # API endpoint definitions
│   │   ├── services/           # SafetyService (classifier + crisis response)
│   │   ├── config.py           # All configuration (DB URL, JWT, OpenAI model)
│   │   └── main.py             # Entrypoint
│   └── requirements.txt
├── mobile/
│   ├── src/
│   │   ├── constants/          # Global styling constants
│   │   ├── context/            # AuthContext (global session state)
│   │   ├── navigation/         # React Navigation stacks
│   │   ├── screens/            # ChatScreen (crisis-aware), HomeScreen, CrisisSupportScreen
│   │   └── services/           # conversations.ts, auth.ts, api.ts
│   ├── App.tsx
│   └── app.json
```

## Security

- Authentication and authorization are enforced at the API layer.
- Sensitive tokens are stored securely on-device.
- Backend never exposes raw credentials or secrets.
- Passwords are hashed using PBKDF2 with no platform-specific dependencies.

## Future Work

- **Conversation history as LLM context** — Pass the last N messages to give the model memory within a session
- **Memory retrieval** — Inject semantically relevant memories from past conversations
- **Streaming responses** — Token-by-token streaming to the frontend
- **Additional LLM providers** — Anthropic, Ollama (local), or Azure as `LLMProvider` alternatives
- **Tool orchestration** — Let the LLM invoke structured tools (e.g., mood logging, calendar)
- **Advanced safety policies** — ML-based classifier to replace keyword matching
